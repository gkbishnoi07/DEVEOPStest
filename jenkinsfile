pipeline {
    agent any
    
    stages {
        
        stage('Install Dependencies') {
            steps {
                echo 'npm install'
            }
        }
        
        stage('Run Tests') {
            steps {
                echo 'npm test || echo "No tests defined"'
            }
        }
        
        stage('Build Application') {
            steps {
                echo 'npm run build'
            }
        }
    }
    
}
